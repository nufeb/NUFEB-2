int main(int argc, char* argv[]) {

	// Initialize model

	double time = 0;  // Initialize time
	double limit = 100; // Initialize time limit
	double step = 1; // Initialize time step
	while (time < limit) {
		time += step;  // Advance time
		// Update model
		// Biological evolution
		// Fluid phase model
		// Particle based physics model
	}
	return 0;
}
